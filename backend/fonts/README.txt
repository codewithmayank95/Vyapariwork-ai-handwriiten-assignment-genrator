Place your handwriting font here.

Recommended:
- backend/fonts/Kalam-Regular.ttf

The generator will automatically use Kalam if present.
If not present, it falls back to a default font so the MVP still works.
